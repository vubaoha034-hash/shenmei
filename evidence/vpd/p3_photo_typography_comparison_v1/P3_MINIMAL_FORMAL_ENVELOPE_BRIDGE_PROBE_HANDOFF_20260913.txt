P3 MINIMAL FORMAL-ENVELOPE BRIDGE PROBE — TECHNICAL DIAGNOSTIC ONLY

Purpose
- Test whether the SHANJUN_A_FINAL direct-reference content envelope remains stable when isolated from the full VPD/P3 project bootstrap.
- NOT a formal P3 sample, NOT aesthetic evidence, NOT Capsule validation.
- Exactly one image call. No retry in the same chat.

PRECONDITIONS
- Probe 1 PASS: base mushroom subject + exact 3:4 portrait works without reference.
- Probe 2 PASS: minimal reference-present path still returns correct mushroom subject + exact 3:4 portrait.

CRITICAL ISOLATION
- Run in a brand-new standalone minimal chat, preferably outside the project.
- Before the image call, do NOT read START_HERE, roadmap, task lock, checkpoint, P3 A/B handoff, Capsule, Figma evidence or prior generated images.
- The only image available in that chat must be the exact approved reference:
  filename R1C-APPROVED-SHANYEJI-CANONICAL.jpg
  Drive File ID 1fG2OQ7IphZfGnH1csu1qZKYCsyAMDOAZ
  expected SHA-256 9a29fbdc7dd908017924bed270e8dfe18351519eed3fa7bc7001789f2a383414
- Use the reference only as a visual-quality/editorial anchor, never as an edit target.

ONE IMAGE REQUEST
Use only this visible request:

“参考我刚刚上传并核验的图片，只学习它成熟的商业摄影与编辑视觉质感，不复制其中的文字、版式、品牌、符号或具体场景。

生成一张独立的写实商业摄影照片。唯一主体是新鲜的时令野生菌菇/食用菌，处于可信的采收或备料环境中；不得出现炒锅、主动烹饪或火焰表演。

画面需要真实的近景/中景/远景层次和可信材质，让场景本身自然形成一块可供后续排版使用的安静区域，但不要预设它必须位于左、右、上或下，也不要人为制造黑色渐变区、米色文字板、横幅或空白面板。

保持克制、真实的商业摄影后期。不要出现花瓶、花枝、书本、杯子、豆腐、人物、任何文字、Logo、标签、徽章、拼版、多面板或海报框。

必须是3:4竖版、单张、满版摄影，不是海报。只生成1张。”

TOOL BINDING
- n = 1
- explicit 3:4 portrait size; 1086x1448 preferred, exact equivalent 3:4 accepted if recorded
- exact approved reference available in the same chat
- no other reference or prior generated image
- no style-transfer/edit target

PASS ONLY IF ALL ARE TRUE
1. exactly one standalone image;
2. subject is edible/seasonal mushrooms in a credible harvest/preparation field;
3. no active wok/flame cooking spectacle;
4. no prohibited objects/text/logo/collage/panels;
5. exact 3:4 portrait geometry;
6. no silent retry.

FAIL
- If any condition fails, STOP. Do not retry in that chat.
- Classification: MINIMAL_FORMAL_ENVELOPE_BINDING_FAILURE.
- Formal P3 remains blocked.

PASS INTERPRETATION
- If this probe passes, base subject/aspect, minimal reference presence, and the A-arm formal content envelope all work in isolation.
- The prior two failed formal fresh executions are then best localized to full-project/handoff orchestration or context-loading behavior, not to mushroom subject, 3:4 geometry, the reference image itself, or the isolated A-arm brief.
- After PASS, prepare a micro-handoff execution architecture: one brand-new minimal chat per formal output, loading only the exact approved reference plus that one output’s frozen arm-specific brief. Do not preload the full project.

AFTER THE PROBE
- Return the output to the project chat for dimension/SHA/content verification.
- Do not score aesthetics.
- Do not use the probe pixels in Figma or P3 A/B comparison.
