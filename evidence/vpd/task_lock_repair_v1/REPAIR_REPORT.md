# VPD 任务身份与状态修复

任务：`VPD-TASK-LOCK-REPAIR-20260907-001`。父任务保持 `VPD-SYSTEM-LEVEL-HOLDOUT-TRANSFER-VALIDATION-V1`。

起始远端提交：`8432f28203845f2c419de59eb4a3d8a5d862258e`；仓库 `vubaoha034-hash/shenmei`，分支 `visual-program-distillation-v2-photography-design-20260814`。
本记录所在的修复提交是实施版本；提交后的远端回读必须另由真实工具证明，不能从这份文件自证。

## 已完成

- 在当前聊天完成有界控制修复，不需要用户转交另一个 Codex 任务。
- 唯一任务锁：`continuity/vpd/CURRENT_TASK_LOCK.json`。根入口、AGENTS 和 adapter 统一引用它；原校验入口 `python scripts/verify_visual_memory.py --vpd-state --status-card` 实际读取同一锁和检查点。
- 原 Drive `approved_refs` 18 条文件定位已逐条匹配。现有批准证据保留；未重新取得的原图字节哈希继续为未知，没有凭文件名新增权限。其他合法源图没有删除。
- 山野集仍是当前家族；V1.1 Candidate 没有晋升。反馈例图、生成图、诊断图没有变成母参考。
- 第 22 号检查点、当时 adapter 和系统账本原字节保存在 `continuity/vpd/history/sequence_22/`；当前推进至第 23 号，授权和冻结不再同时属于已完成和待办。
- 追加外部执行异常待对账事件。聊天报告的四张晨席输出没有被清零或包装成四张有效试验图；调用内容、绑定和实际输出文件仍未核实。上下文污染仅保留为未验证假设。
- 旧指令不自动重放；准备、调用、返回、核验和人工接受分别记录。
- 28 项新增测试通过，覆盖 12 类要求以及实际入口成功/拒绝。75 个被冻结域文件的实际仓库字节未改变。

## 测试结果与边界

原基线：143 通过、7 失败。修复后：171 通过、同样 7 失败，新增回归 0。
7 项失败来自四个旧文件的预期哈希采用 CRLF，而 Git 原文为 LF；本次没有改原文、冻结哈希或削弱断言。
两项把最新状态硬锁在第 22 号的历史准备测试，改为读取保留的第 22 号原字节快照；所有断言保持原样，当前状态由新增检查验证。

本地校验能拒绝不合规状态和成功声明，不能控制图像服务端，也不能证明图片好看。

## 未完成

- 第二次正式渲染的实际调用、附件绑定、输出身份和像素对账。
- 有效的四张迁移结果、独立视觉评审和用户最终接受。
- 稳定商业质量、候选晋升、第二个未见风格家族验证及规模化。
- 上述历史跨平台换行哈希问题的正式处理。

## 保持的流程

原已批准海报真实像素 → 摄影、字体、构图等设计规律 → 当前家族视觉程序 → 明确的新内容/材质/比例任务 → 出图并记录实际调用及文件 → 先确认执行身份，再检查视觉质量与迁移 → 用户接受 → 需要时进行 Figma 等可编辑生产 → 使用同一蒸馏器验证另一个风格家族。

直接参考图加简短指令的同模型对照仍由原总纲规定；流程更复杂不能自动算胜过这个对照。

当前唯一下一动作：`CHATGPT_REVIEW_TASK_LOCK_AND_STATE_READBACK`。
暂停新渲染、第三次尝试、S1 重启和 Figma 精修。本次修复不等于蒸馏质量已通过。
