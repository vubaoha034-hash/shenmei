# V3 Route A — Family Transfer Validation

Status: `FROZEN_NEXT_VALIDATION`

Repository: `vubaoha034-hash/shenmei`
Required branch: `visual-program-distillation-v2-photography-design-20260814`

## Purpose

Validate that V3 Route A learned a transferable visual-family program rather than merely reproducing Reference 05's macro board layout.

The previously validated artifact:
`V3-A_STRUCTURE_LOCKED_BRAND_WORLD.png`

has status:
`STRUCTURAL_BREAKTHROUGH / ABSOLUTE_NOT_FINAL / GENERALIZATION_UNPROVEN`

It materially escaped the old `large brush title + food plate` collapse, but it follows the mother reference's macro skeleton too closely:

- top process hero;
- middle red campaign/module band;
- bottom restaurant-world section.

The next output must preserve family identity while making that exact macro skeleton impossible.

## Inputs

Use:

- canonical `R1C-APPROVED-05.jpg` as Mother Reference;
- the same real main-dish content asset used in V3 A;
- the same real multi-dish / content asset used in V3 A;
- `V3-A_STRUCTURE_LOCKED_BRAND_WORLD.png` may be shown only as a system-proof / Golden Exemplar of what already worked structurally, but its macro layout is explicitly forbidden as the next layout.

Do not add more reference images or library material.

## Core family signature to preserve

The new composition must visibly preserve at least four of the following five mechanisms:

1. `PROCESS_SCENE_HIERARCHY`
   - cooking/action/process imagery materially participates in the identity;

2. `MULTI_TIER_INFORMATION_SYSTEM`
   - at least three readable hierarchy levels beyond a single headline;

3. `CAMPAIGN_MODULE_LOGIC`
   - supporting ingredient/process/origin/story modules exist as part of one system;

4. `SEMANTIC_GRAPHIC_DERIVATION`
   - graphic marks derive from cooking/ingredient/fire/origin semantics, not generic decoration;

5. `DINING_WORLD_CONTEXT`
   - the brand world extends beyond one dish into kitchen, dining, origin, process, or cultural context.

## Macro-layout anti-copy constraints

The new output MUST NOT use the following architecture:

- full-width process hero at top;
- horizontal red band in the middle containing three rectangular modules/cards;
- full-width restaurant/world scene at bottom.

The new output MUST NOT reproduce the same top/middle/bottom proportions, same three-card campaign band, or same bottom icon rail as either the Mother Reference or `V3-A_STRUCTURE_LOCKED_BRAND_WORLD.png`.

The visual model must choose a genuinely different macro composition while preserving the family signature.

Permitted examples of structural variation include, but are not limited to:

- asymmetric dominant process image with an editorial vertical information rail;
- one full-bleed kitchen/process scene with embedded campaign chapters and semantic marks;
- diagonal or offset image fields with a compact brand-world inset;
- modular composition using unequal image masses and one strong visual axis rather than three horizontal tiers.

Do NOT select one by random list. Let the strongest visual model choose the composition that best fits the real content while satisfying the anti-copy constraints.

## Typography

Do not return to generic brush-calligraphy.

Do not mechanically reuse the exact block-title treatment from V3 A.

Typography must serve the new composition and hierarchy. Display type may be custom/expressive, but supporting copy must remain readable and disciplined.

## Visual attachment budget

Keep the generation input small and non-redundant.

Maximum visual attachments for the formal generation:

- Mother Reference: 1
- real content assets: max 2
- Golden Exemplar: max 1
- role crops: max 1 only if genuinely non-redundant

Total max formal visual attachments: 5.

If a role crop duplicates information already visible in the Mother Reference or Golden Exemplar, omit it.

## Formal output count

Generate exactly one formal output:

`V3-A_FAMILY_TRANSFER_VALIDATION.png`

No hidden variants.
No best-of-N.
No aesthetic retries.

If the model cannot satisfy the anti-copy constraints without losing family identity, fail closed with:

`V3_A_FAMILY_TRANSFER_FAILED.json`

rather than generating a compromised generic poster.

## Technical geometry

Use the already-proven renderer-native portrait geometry contract if the same renderer is used.
Do not crop/resize a mismatched output into compliance.

## Pre-generation self-check

Before generating, answer internally:

1. If color were removed, would the proposed composition still clearly differ from a generic restaurant poster?
2. If the Mother Reference were hidden after generation, would at least four family mechanisms remain visible?
3. Is the macro layout clearly different from both Reference 05 and V3 A?
4. Does the composition remain one authored visual system rather than a moodboard assembled from unrelated rectangles?

If any answer is NO, revise the renderer direction before the one formal generation.

## Post-generation technical gate only

Codex may check:

- image bytes valid;
- dimensions/geometry contract;
- no catastrophic text corruption;
- no catastrophic image artifacts;
- formal output count = 1;
- hidden outputs = 0.

Codex MUST NOT declare aesthetic success.

## Human visual validation

The output must be uploaded unchanged to Drive with canonical Mother Reference 05 and V3 A for visual comparison.

Human review must answer:

- family identity preserved? yes/no;
- macro-layout copy broken? yes/no;
- generic-template collapse returned? yes/no;
- authorship improved, neutral, or regressed?;
- typography improved, neutral, or regressed?;
- absolute final-use quality: usable/unusable;
- top-tier: true/false.

## Pass definition

`FAMILY_TRANSFER_SUPPORTED` requires all:

- family identity preserved;
- macro-layout copy broken;
- at least four core family mechanisms visible;
- no return to `large brush title + food plate` collapse;
- reference-family structural fidelity >= 7/10;
- generic-template collapse <= 4/10;
- absolute aesthetic quality is at least as strong as V3 A;
- no major technical regression.

Reaching final-production `USABLE` is desirable but not required for this specific generalization test; if still unusable, record `TRANSFER_SUPPORTED / FINAL_QUALITY_NOT_READY`.

## Prohibited

Do not:

- change Discovery;
- enter Library Scale Gate;
- bulk import references;
- add global production rules;
- create Route B output;
- use Figma/manual post-processing;
- make more than one formal image.

Route B remains `SOURCE_MISMATCH_FOR_ROUTE_B` until compatible real vessel + raw-ingredient still-life source material exists.

## Final response

Return only:

# V3 A FAMILY TRANSFER READY FOR HUMAN VALIDATION

Branch:
HEAD:
Formal outputs:
Hidden outputs:
Technical retries:
Output filename:
Output SHA-256:
Dimensions:
Drive folder:
Mother Reference attached:
V3 A Golden Exemplar attached:
Formal attachment count:
Role crops used:
Global compiler changed:
Route B status:
Scale Gate executed:

## 已完成什么

## 未完成什么

## 需要人工验收什么

Then STOP.
