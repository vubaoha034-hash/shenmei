# Repository instructions for Codex

版本：`4.2.0`
状态：`MANDATORY`

先读取根 `START_HERE.md`，再按任务类型选择路线。不得因为旧文件仍存在，就让旧 Figma-first 规则覆盖 V4.2 每日品牌案例。

## 1. 每日或单次完整品牌案例｜默认主路线

凡涉及以下任一需求，必须执行 V4.2：

- 每天 09:00 自动生成品牌案例；
- 系统自主确定餐饮品牌；
- 一套十张品牌设计案例；
- 参考案例图的多模块整版呈现；
- 空间、门头、品牌字标、海报、包装和应用的完整展示；
- 用户明确说不用 Figma。

必须读取：

- `DAILY_AUTOBRAND_SYSTEM_V4.md`；
- `rules/DAILY_CASE_10_IMAGE_STRUCTURE_V4.md`；
- `rules/BOARD_LAYOUT_PRESENTATION_RULES_V4.md`；
- `rules/BRAND_WORDMARK_AND_TYPOGRAPHY_RULES_V4.md`；
- `rules/DAILY_CASE_QUALITY_GATE_V4.md`；
- 每日任务读取 `schedules/DAILY_0900_BRAND_CASE_V4.md`。

## 2. 强制行为

1. 读取最近 7 个有效项目的轻量记录；
2. 避开近期重复的品类、品牌名核心字、视觉隐喻、品牌人格和色彩公式；
3. 自主确定一个差异明确的品牌，形成 `产品事实 + 消费场景 + 品牌观点 + 视觉机制`；
4. 主 Logo 必须进行定制字形设计，不能直接套现成字体；
5. 先建立十张页面角色清单、文字密度表、物料出现表和 `color_strategy_map`；
6. 每页生成一张独立的完整横版案例展示图；
7. 默认比例 `3:2`，推荐 `2400 × 1600 px`，PNG；
8. 每页只设一个第一视觉中心，通常使用 1 个主模块和 0—3 个辅助模块；
9. 全套仅允许 1—2 张中等文字密度页面，其余页面必须少字；
10. 配角物料分配到唯一专属页面，完整展示后不得跨页重复作为主内容；
11. 页面内不得显示 `01`、`02`、`03` 等序列号、页码或提案式章节编号；
12. 十张共同覆盖空间、门头、字标、超级符号、故事、产品、食材、海报、包装、运营触点、产品组合和总结；
13. 先定义品牌色彩性格，再分配每页底色、图像色温、文字对比和强调色；
14. 十张不得全部使用暖米底、奶油底、木棕摄影或同一暖色生活方式公式；
15. 逐张检查中文、品牌名、字标一致性、空间、食品、包装、物料结构、文字密度、跨页重复和色温节奏；
16. 只有 10 个独立文件全部通过，才使用 `READY`。

## 3. 主角与配角

允许适度跨页重复的品牌主角：

- 定制中文主字标；
- 核心超级符号；
- 主产品；
- 核心空间气质。

原则上只重点出现一次的配角：

- 手提袋、打包盒、杯、碗和餐具包；
- 围裙、员工服；
- 桌牌、取餐牌、菜单牌；
- 小票、会员卡、吊牌、贴纸、封签；
- 其他运营应用物料。

主角重复时也不得机械复用同一张图片、同一裁切和同一模块比例。

## 4. 文字密度

- 中等文字密度页面最多 2 张，通常为品牌元素页和品牌故事页；
- 其余页面不得出现正文段落；
- 低文字页面只保留品牌名、必要标题、短句和少量标签；
- 海报页中的海报文案属于设计对象，但页面外围不得再堆说明文字；
- 不得用微型中文、无意义英文、细线和编号填满留白。

## 5. Color Temperature and Background Policy V4.2

### 5.1 先建立 `color_strategy_map`

必须记录：

```text
brand_color_personality
base_background_families
image_temperature_plan
background_temperature_plan
accent_color_plan
recent_color_formula_to_avoid
```

不得跳过该步骤直接生成暖米色页面。

### 5.2 禁止默认暖色模板

- 暖米色、奶油色、燕麦色、黄棕色不是默认背景；
- 不得把“暖光空间 + 暖色食品 + 暖米底 + 棕字”当作普适高级感；
- 连续项目不得重复同一暖色纸张、木质空间和日光滤镜公式；
- 面包、粥铺、家常、手作等品类也不能自动等同于全套暖色。

### 5.3 跨页温度与明度节奏

每套十张至少出现三种背景状态：

- 冷白、灰白、雾蓝灰、浅灰绿或矿物灰等冷/中性浅底；
- 深灰、炭黑、深绿或其他深色高对比底；
- 由空间、食品或场景图像主导的页面。

暖色页面可以存在，但原则上只占少数。不得机械按固定数量分配；必须由页面角色和品牌事实决定。

### 5.4 四层不得同时偏暖

主图、页面底色、文字色和强调色不得在多数页面同时偏暖。

- 主图偏暖时，优先使用冷白、中性灰、浅灰绿或深色背景；
- 背景偏暖时，图像、文字或强调色至少一层保持中性或偏冷；
- 深色页面必须保留食物材质与暗部层次，不能死黑；
- 冷色页面不能把食物修成无食欲的青灰色。

### 5.5 品牌专属而非配额化

不要为了“有变化”机械套用：

- 2 张冷色；
- 2 张中性；
- 2 张深色；
- 2 张暖色。

目标是建立品牌专属色彩性格和页面节奏，而不是完成色彩配额。

## 6. 绝对禁止

- 以一张十页合板、十宫格、长图或总览图代替 10 张独立图片；
- 默认使用 4:5 竖版；
- 要求用户自行拼版；
- 将 Figma 设为依赖、前置条件或验收条件；
- 因没有 Figma URL、file_key 或 node_id 阻塞任务；
- 直接使用现成字体冒充最终 Logo；
- 十张中的品牌名字形发生漂移；
- 页面显示序列号或页码；
- 每页都写大段说明；
- 袋子、围裙、杯子、菜单牌、小票等配角跨页反复出现；
- 十张全部为暖米底、奶油底、木棕或同一暖光滤镜；
- 图片暖、背景暖、文字暖、强调色也暖，导致层次糊成一片；
- 用错字、乱码、无意义英文、假电话、假二维码、假地址或假奖项制造专业感；
- 复制参考案例的品牌名、字标、超级符号、水印、设计公司页脚或完整商业外观；
- 十页复制同一版式模板；
- 连续项目只替换“灶、山野、火、院子”等相似语义而没有真正改变方向。

## 7. 默认输出

- 数量：10 张独立图片；
- 比例：统一 `3:2` 横版；
- 推荐像素：`2400 × 1600 px`；
- 格式：PNG；
- 页面内无序列号；
- 文件顺序由 `CASE_MANIFEST.json` 管理；
- `CASE_MANIFEST.json` 必须包含 `color_strategy_map`、`text_density_map` 和 `asset_appearance_map`；
- 用户指定尺寸时，以用户要求为准。

## 8. Figma 专项路线｜仅明确要求时使用

只有用户明确要求可编辑 Figma 文件、组件库、变量、node ID 或设计团队协作源文件时，才读取旧 Figma 路线。旧 Figma validator 不约束 V4.2 每日品牌案例。

## 9. 单张餐饮海报与视觉素材

读取 `skills/restaurant-poster-art-director/SKILL.md`、`generation/RESTAURANT_POSTER_IMAGE_RULES_V1.md` 和相关配置。独立海报多风格配额不得强加到同一品牌十张案例中。

## 10. 私人审美与餐饮经营

正式审美评分读取对应 Skill、rubric、penalties、schema 和 calibration。餐饮经营读取经营系统入口和当前真实数据。视觉通过不能覆盖经营、食安或盈利问题。

## 11. V4.2 最终检查

```text
[ ] 正好10张独立图片
[ ] 全套统一3:2横版，或符合用户明确指定尺寸
[ ] 页面内没有01、02、03等可见序列号
[ ] 不是一张总览合板
[ ] 存在可解释的定制字标系统
[ ] 十张品牌名和字标完全一致
[ ] 只有1—2张中等文字密度页面
[ ] 其余页面没有正文段落
[ ] 配角物料没有跨页重复作为主要内容
[ ] 已建立color_strategy_map
[ ] 没有把暖米色和暖光当作整套默认答案
[ ] 至少出现三类背景状态
[ ] 主图、底色、文字和强调色未在多数页面同时偏暖
[ ] 十张功能完整且至少使用四种版式骨架
[ ] 中文和拼音可读
[ ] 食品、空间、包装和物料无明显结构错误
[ ] 没有复制参考项目的水印、署名或商业外观
[ ] 最终状态为READY
```

任何一项未通过，不得声称任务完成。
