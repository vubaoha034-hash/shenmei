# VPD V2 Contrastive Signature Ablation — Attempt 1 Technical Invalidation

Status: `TECHNICALLY_INVALID / NO_AESTHETIC_INFERENCE`

## Frozen failed-attempt facts

The first execution of `VPD_V2_CONTRASTIVE_SIGNATURE_ABLATION_TASK.md` stopped before a complete four-condition benchmark existed.

Reported execution state:

- valid outputs: `3/4`;
- hidden outputs: `0`;
- technical retries: `2`;
- Drive review folder: not created;
- blind labels: not released;
- mapping remained private;
- global compiler changed: `NO`;
- role-specific reference crops used: `NO`;
- Library Scale Gate executed: `NO`.

The blocked locked request returned `1003×1568` on three consecutive attempts instead of the frozen benchmark canvas geometry.

## Interpretation

This is a renderer/output-geometry contract failure, not evidence for or against `DISCRIMINATIVE_FEATURE_SELECTION_FAILURE`.

No aesthetic or causal conclusion may be drawn from the three otherwise valid outputs.

Do not:

- blind-review the three outputs;
- reuse them in a replacement benchmark;
- combine them with later replacement outputs;
- infer that Control or Treatment won;
- modify the contrastive-signature hypothesis because of this failure.

## Why identical retry is prohibited

Three consecutive realizations of the same locked request returned the same unsupported geometry class. Repeating the unchanged request again would not be a meaningful technical retry; it would be repeated sampling against an uncorrected renderer contract.

The next execution must first establish a renderer-native geometry contract that the current tool can actually guarantee, then rerun all four conditions fresh under that same geometry contract.

## Replacement boundary

A clean replacement run must:

1. quarantine all Attempt-1 outputs from aesthetic review;
2. perform renderer-geometry capability/preflight resolution;
3. freeze one native portrait geometry contract shared by all four conditions;
4. regenerate all four conditions from scratch;
5. keep the original contrastive-signature experiment variable unchanged;
6. keep mapping private until independent blind review.

The replacement must not use output geometry as an aesthetic optimization variable.
