# CHANGELOG V4.1—V4.2

## V4.1｜结构、文字与跨页去重

- 默认单页由 `4:5` 竖版改为 `3:2` 横版，推荐 `2400 × 1600 px`；
- 最终页面禁止出现 `01`、`02`、`03` 等序列号或页码；
- 全套仅允许 1—2 张中等文字密度页面，其余页面以图像为主；
- 配角物料建立唯一主展示页，袋子、围裙、杯子、菜单牌、小票、会员卡等不得跨页反复作为主要内容；
- 只有主字标、超级符号、主产品和核心空间气质允许适度跨页重复；
- 第 9 张改为产品组合、菜单气质或品类矩阵，避免重复包装和应用物料；
- 总结页只回顾品牌主角，不再把所有配角缩小排列；
- 近 7 个项目新增品牌名语义和视觉隐喻去重，避免连续使用“灶、山野、火、院子”等相似方向；
- 文件使用语义角色名，页面顺序由 `CASE_MANIFEST.json` 管理。

## V4.2｜色彩人格、背景家族与冷暖节奏

### 核心变化

- 暖色不再是默认品牌答案；
- 暖米色、奶油色、黄棕纸张底不再是默认页面背景；
- 每个项目在生成前必须定义 `brand_color_personality`；
- 每套十张必须建立 `background_families`、`temperature_map`、`brightness_map` 与 `contrast_plan`；
- 最近 7 个项目的审计新增整体色温、背景家族和暖色惯性；
- 最近两套若均为暖调，下一套优先采用冷、中性或深色主导方向；
- 同一页面不得让主图、背景、文字和强调色全部同时偏暖；
- 暖色允许使用，但必须是有品牌依据的局部节奏。

### 默认数量约束

除非 `color_exception_reason` 有明确品牌证据：

- 暖米色、奶油色、黄棕纸张底不得超过 3 张；
- 至少 4 张不得使用暖米或奶油底；
- 至少 2 张使用冷色或中性底；
- 至少 1 张使用深色、低明度或强对比背景；
- 十张至少使用三类背景家族；
- 不得连续 3 张使用相同背景家族；
- 不得连续 3 张使用相同色温；
- 任何单一背景家族默认不得超过整套的 50%。

### 新增常规背景方向

- 冷白；
- 灰白；
- 雾蓝灰；
- 浅灰绿；
- 矿物灰；
- 深灰；
- 炭黑；
- 中性纸白；
- 品牌色实底；
- 局部暖底。

### 新增失败状态

- `REJECTED_WARM_COLOR_DEFAULT`：整套持续依赖暖米底、奶油色、暖木色和金黄光线；
- `REJECTED_RECENT_PROJECT_DUPLICATION`：近期项目持续重复同一色温、背景家族或暖色模板。

### V4.2 通过标准

一套通过的品牌案例必须同时体现：

- 品牌专属色彩人格；
- 至少三类背景家族；
- 清楚的冷、暖、中性与深浅节奏；
- 食品、背景、文字和强调色之间的有效对比；
- 色彩变化服务页面角色，而不是随机换色；
- 暖色是有理由的局部选择，不是安全模板。
