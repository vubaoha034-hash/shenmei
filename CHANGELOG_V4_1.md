# CHANGELOG V4.1 → V4.2

## V4.1｜结构、文字与配角去重

- 默认单页由 `4:5` 竖版改为 `3:2` 横版，推荐 `2400 × 1600 px`；
- 最终页面禁止出现 `01`、`02`、`03` 等序列号或页码；
- 全套仅允许 1—2 张中等文字密度页面，其余页面以图像为主；
- 配角物料建立唯一主展示页，袋子、围裙、杯子、菜单牌、小票、会员卡等不得跨页反复作为主要内容；
- 只有主字标、超级符号、主产品和核心空间气质允许适度跨页重复；
- 产品组合页不再重复包装和应用物料；
- 总结页只回顾品牌主角，不再把所有配角缩小排列；
- 近 7 个项目新增品牌名语义和视觉隐喻去重，避免连续使用“灶、山野、火、院子”等相似方向；
- 文件使用语义角色名，页面顺序由 `CASE_MANIFEST.json` 管理。

## V4.2｜色彩性格、背景温度与反暖色惯性

### 核心问题

V4.2 专门解决以下重复倾向：

- 不同品牌项目默认使用暖米色或奶油色底；
- 空间、食品、包装、文字和强调色同时偏暖；
- 面包、粥铺、手作、社区餐厅等品类自动进入同一种温暖生活方式模板；
- 连续项目反复使用暖木空间、金黄食品、棕绿色文字和柔和日光叶影；
- 整套十张缩略观看时几乎是同一块暖米色。

### 新增硬规则

- 暖色和暖米色底不再是默认设计答案；
- 每个项目生成前必须建立 `color_strategy_map`；
- `color_strategy_map` 必须记录品牌色彩人格、背景家族、图像色温、背景温度、强调色、对比策略和近期禁用色彩公式；
- 十张至少出现冷或中性浅底、深色或高对比底、场景图像主导三类背景状态；
- 暖色页面原则上只占少数；若超过一半，必须说明品牌理由并建立冷色、中性或深色对冲；
- 主图、页面底色、文字和强调色不得在多数页面同时偏暖；
- 冷白、灰白、雾蓝灰、浅灰绿、矿物灰、深灰、炭黑、深绿和深蓝进入常规背景系统；
- 不得为了规避暖色而把食品修成青灰、发霉、死黑或无食欲；
- 最近 7 个项目新增色彩公式、背景温度、空间光线和食品摄影温度去重；
- 连续复用“暖米纸底 + 暖木空间 + 金黄食品 + 棕绿文字 + 叶影日光”将直接判定失败。

### 新增失败状态

- `REJECTED_MISSING_COLOR_STRATEGY`；
- `REJECTED_DEFAULT_WARM_FORMULA`；
- `REJECTED_FLAT_TEMPERATURE_RHYTHM`；
- `REJECTED_ALL_WARM_LAYERS`。

### 最终判断标准

> 色彩统一来自品牌逻辑，不来自模型惯用滤镜；温暖可以是选择，但不能是每个项目的默认答案。
