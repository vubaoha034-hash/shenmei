# CHANGELOG V4.1

## 核心更新

- 默认单页由 `4:5` 竖版改为 `3:2` 横版，推荐 `2400 × 1600 px`；
- 最终页面禁止出现 `01`、`02`、`03` 等序列号或页码；
- 全套仅允许 1—2 张中等文字密度页面，其余页面以图像为主；
- 配角物料建立唯一主展示页，袋子、围裙、杯子、菜单牌、小票、会员卡等不得跨页反复作为主要内容；
- 只有主字标、超级符号、主产品和核心空间气质允许适度跨页重复；
- 第 9 张改为产品组合、菜单气质或品类矩阵，避免重复包装和应用物料；
- 总结页只回顾品牌主角，不再把所有配角缩小排列；
- 近 7 个项目新增品牌名语义和视觉隐喻去重，避免连续使用“灶、山野、火、院子”等相似方向；
- 文件使用语义角色名，页面顺序由 `CASE_MANIFEST.json` 管理。