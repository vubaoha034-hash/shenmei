# R1D — ART DIRECTION APPLICATION PROTOCOL

Status: `PRE_REGISTERED / SINGLE SHOT`
Date: `2026-08-14`
Base: `R1C = ART_DIRECTION_LOCKED`

## Goal

Apply the locked `STOVE-RIM EDITORIAL` Art Direction exactly once to the frozen R1 food asset in Figma, then stop for independent pixel review.

## Inputs

- Frozen food asset: `R1_SINGLE_DISH_HERO_CANDIDATE.png`
- Drive file ID: `1ptSMiN7b88DEvbmWdQ7l4Td_BTo-PWh8`
- Figma file: `LIU_VISUAL_R1B_DESIGN_LAYER_PROOF`
- Figma file key: `IGJRXAlQLtlKXqC1ljNLGg`
- Art Direction authority: `R1C_ART_DIRECTION_LOCK_RESULT.md`

## Frozen rules

- No ImageGen.
- No inpaint/redraw of food.
- No second variant.
- No selective retry because the design looks weak.
- Exactly one new 4:5 Figma frame.
- Real editable Chinese text only.
- Frozen raster may only be cropped, scaled, positioned and non-destructively tone-matched.

## Required art direction

Execute `STOVE-RIM EDITORIAL` exactly as frozen:
- asymmetrical food crop, roughly 55–65% visual mass;
- deliberate breathing zone in upper-left / upper field;
- product-derived mechanism: partial pan-rim arc + heat-trail behavior + sparse chili-red point rhythm;
- real editorial title composition for `今日现烧`;
- quiet supporting line `锅气刚好，趁热吃`;
- warm mineral off-white + charcoal + deep fermented-chili red only;
- no large orange geometry;
- no generic borders, unrelated rectangles, traditional ink/brush/seal shortcuts.

## Typography

Use a real installed Chinese display/serif/humanist face with some handmade energy if available. If unavailable, use a real Chinese serif fallback. Do not use a generic heavy sans block as the dominant display solution.

`今日现烧` must not be one uniform line. Build hierarchy inside the phrase.

## Supporting graphics

Maximum 3 small supporting marks. Every mark must have source logic from pan rim, heat path or chili/ingredient point rhythm.

## Output

Create exactly one new Figma frame and export exactly one PNG:

`R1D_STOVE_RIM_EDITORIAL_PROOF.png`

Upload to:

`LIU_VISUAL_REVIEW/R1D/`

## Review

Codex may verify:
- editable layers;
- frozen image SHA lineage;
- actual export dimensions;
- real text;
- no ImageGen;
- exactly one layout.

Codex must not declare visual success.

Independent pixel review remains `PENDING` until ChatGPT opens the Drive PNG.

## Stop rule

After export/upload, STOP.
Do not create a second version.
Do not enter packaging/R2.
Do not update Discovery.